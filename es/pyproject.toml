[project]
name = "es"
version = "0.1.0"
description = "EverStone agent tool-gateway CLI"
requires-python = ">=3.12"
dependencies = ["typer>=0.12", "pyyaml>=6", "everstone-tasks", "google-api-python-client>=2", "google-auth>=2"]

[project.scripts]
es = "es.main:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["es*"]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv.sources]
everstone-tasks = { path = "../everstone_tasks", editable = true }
